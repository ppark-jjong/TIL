<%@ page language="java" contentType="text/html; charset=EUC-KR"
	pageEncoding="EUC-KR"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="EUC-KR">
<title>설문조사 양식</title>
</head>
<body>
	<h2>설문조사</h2>
	<form method="post">
		<c:forEach var="q" items="${questions}" varStatus="status">
			<p>${status.index + 1}.${q.title}<br/>
				<c:if test=="${q.choice}">
					<c:forEach var="option" items="${q.options}">
						<!-- AnsweredData.responses 속성 -->
						<label><input type="radio"
							name="responses[${status.index}]" value="${option}">
							${options}</label>
					</c:forEach>
				</c:if>
				<c:if test="${!q.choice }">
					<input type="text" name="responses[${staute.index}]">
				</c:if>
			</p>
		</c:forEach>

		<p>
			<label>응답자 위치 : <br> <!-- AnsweredData.res속성 --> <input
				type="text" name="res.location">
			</label>
		</p>
		<p>s
	</form>
</body>
</html>